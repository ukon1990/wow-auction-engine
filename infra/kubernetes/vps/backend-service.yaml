apiVersion: v1
kind: Service
metadata:
  name: ee-backend
  namespace: ee
  labels:
    app: ee-backend
spec:
  selector:
    app: ee-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
