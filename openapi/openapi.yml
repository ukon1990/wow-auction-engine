openapi: 3.0.3
info:
  title: WoW Auction Engine API
  version: 0.0.1
  description: Public API for WoW Auction Engine.
servers:
  - url: /api
tags:
  - name: Health
    description: Runtime health endpoints.
  - name: Realm
    description: Realm catalog and per-realm auction house status.
  - name: AuctionMarket
    description: Auction market search and filter metadata.
paths:
  /health:
    $ref: ./paths/health.yml
  /realms:
    $ref: ./paths/realm.yml
  /realms/{region}/{slug}:
    $ref: ./paths/realm-detail.yml
  /auctions/market-search:
    $ref: ./paths/auction-market-search.yml
  /auctions/market-search/filters:
    $ref: ./paths/auction-market-search-filters.yml
