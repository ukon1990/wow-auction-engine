type: object
required: [id, externalEntryId, rankLimit, displayOrder]
properties:
  id: { type: integer, format: int64 }
  externalEntryId: { type: integer, format: int32 }
  name: { type: string, nullable: true }
  description: { type: string, nullable: true }
  rankLimit: { type: integer, format: int32 }
  displayOrder: { type: integer, format: int32 }
