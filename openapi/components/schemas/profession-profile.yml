type: object
required: [characterId, professionId, allocations]
properties:
  characterId: { type: integer, format: int64 }
  professionId: { type: integer, format: int32 }
  treeId: { type: integer, format: int64, nullable: true }
  skillLevel: { type: integer, format: int32, nullable: true }
  allocations:
    type: array
    items: { $ref: "../../openapi.yml#/components/schemas/ProfessionAllocation" }
