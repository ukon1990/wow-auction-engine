type: object
required: [id, expansionId, professionId, externalTreeId, name, tabs]
properties:
  id: { type: integer, format: int64 }
  expansionId: { type: integer, format: int32 }
  professionId: { type: integer, format: int32 }
  externalTreeId: { type: integer, format: int32 }
  name: { type: string }
  description: { type: string, nullable: true }
  tabs: { type: array, items: { $ref: "../../openapi.yml#/components/schemas/ProfessionSkillTreeTab" } }
