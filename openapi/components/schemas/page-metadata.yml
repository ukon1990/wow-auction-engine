type: object
required:
  - page
  - pageSize
  - totalItems
  - totalPages
properties:
  page:
    type: integer
  pageSize:
    type: integer
  totalItems:
    type: integer
    format: int64
  totalPages:
    type: integer
