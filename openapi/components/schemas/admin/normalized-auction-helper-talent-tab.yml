type: object
additionalProperties: false
required: [tabId, nodes]
properties:
  tabId: { type: integer, minimum: 1, description: C_Traits tree ID. }
  name: { type: string, maxLength: 128 }
  description: { type: string, maxLength: 4096 }
  nodes:
    type: array
    maxItems: 1000
    items: { $ref: ./normalized-auction-helper-talent-node.yml }
