type: object
additionalProperties: false
required: [treeId, nodes]
properties:
  treeId: { type: integer, minimum: 1 }
  name: { type: string, maxLength: 128 }
  nodes:
    type: array
    maxItems: 1000
    items: { $ref: ./normalized-auction-helper-talent-node.yml }
