type: object
additionalProperties: false
required: [contractVersion, source, characters]
properties:
  contractVersion: { type: integer, enum: [1] }
  source: { $ref: ./normalized-auction-helper-source.yml }
  characters:
    type: array
    maxItems: 100
    items: { $ref: ./normalized-auction-helper-character.yml }
