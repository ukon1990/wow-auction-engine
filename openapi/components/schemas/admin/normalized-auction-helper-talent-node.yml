type: object
additionalProperties: false
required: [nodeId, entries]
properties:
  nodeId: { type: integer, minimum: 1 }
  maxRanks: { type: integer, minimum: 1 }
  entries:
    type: array
    maxItems: 32
    items: { $ref: ./normalized-auction-helper-talent-entry.yml }
