type: object
required: [id, type, status, startedAt]
properties:
  id:
    type: integer
    format: int64
  type:
    type: string
  status:
    type: string
    enum: [running, completed, failed]
  requestedBy:
    type: string
    nullable: true
  startedAt:
    type: string
    format: date-time
  finishedAt:
    type: string
    format: date-time
    nullable: true
  summary:
    type: object
    additionalProperties: true
    nullable: true
  errorMessage:
    type: string
    nullable: true
