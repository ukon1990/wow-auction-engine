type: object
required: [professionId, knownRecipeCount, allocationCount, sources]
properties:
  professionId: { type: integer, format: int32 }
  skillLevel: { type: integer, format: int32, nullable: true }
  knownRecipeCount: { type: integer, format: int32, minimum: 0 }
  allocationCount: { type: integer, format: int32, minimum: 0 }
  sources:
    type: array
    items: { $ref: "../../openapi.yml#/components/schemas/ProfileCharacterProfessionSource" }
