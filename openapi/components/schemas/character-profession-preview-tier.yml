type: object
required: [skillTierId, skillTierName, skillPoints, maxSkillPoints, knownRecipes]
properties:
  skillTierId: { type: integer, format: int32 }
  skillTierName: { type: string }
  skillPoints: { type: integer, format: int32 }
  maxSkillPoints: { type: integer, format: int32 }
  knownRecipes:
    type: array
    items: { $ref: "../../openapi.yml#/components/schemas/CharacterProfessionPreviewRecipe" }
